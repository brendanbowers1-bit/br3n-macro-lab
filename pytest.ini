[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    slow: long-running integration checks
